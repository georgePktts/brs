import { ChangePriorityPipe } from './change-priority.pipe';

describe('ChangePriorityPipe', () => {
  it('create an instance', () => {
    const pipe = new ChangePriorityPipe();
    expect(pipe).toBeTruthy();
  });
});
